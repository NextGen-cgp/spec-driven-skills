openapi: 3.0.3
info:
  title: <feature-id> API Draft
  version: 0.1.0
  description: Draft generated by sdd-api-contract. Adapt to the project's existing OpenAPI conventions if applicable.
paths:
  /example:
    get:
      summary: Example endpoint
      operationId: exampleOperation
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
        '400':
          description: Validation error
        '403':
          description: Forbidden
components:
  schemas:
    StandardError:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
            details:
              type: object
